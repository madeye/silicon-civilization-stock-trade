DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# live signals
DEEPSEEK_MODEL=deepseek-v4-pro
# cheaper for backtest sweeps
DEEPSEEK_MODEL_BACKTEST=deepseek-v4-flash
DEEPSEEK_BASE_URL=https://api.deepseek.com
PYSERVER_URL=http://localhost:8001
# Protects POST /api/universe/refresh (writes universe.json + spends DeepSeek
# tokens). Optional in dev; REQUIRED in production — without it the endpoint
# returns 403 there. Callers must send the value in the x-refresh-token header.
# UNIVERSE_REFRESH_TOKEN=change-me
