# 超跌分批交易策略（oversold-v1）

目标是等待主题股超跌后参与反弹，常态保持半仓以下。以下阈值是本项目的初始规则，不是经过收益最大化调参得到的结论。

| 项目 | 执行规则 |
|---|---|
| 常态总仓位 | 最多45%，以股票市值 /（现金＋股票市值）计算，预留5个百分点缓冲；允许空仓，不主动补满 |
| 买入条件 | 至少60根有效日线，RSI14≤30、收盘价/MA20−1≤−8%、距近60日最高收盘价回撤≥15%，三项同时满足 |
| 深度超跌 | RSI14≤20、MA20偏离≤−12%、60日回撤≥25%，三项同时满足 |
| 放宽至80% | 至少80%的股票池成员有有效且未停更的数据；合格深度超跌股至少3只，并占有效样本至少30% |
| 单股与分批 | 单股最多总资产15%；每股每次新增投入（含费）最多总资产5%；默认每日评估，最多6只；候选等权目标合计不超过当日组合上限 |
| 基本面约束 | ST/退市标记、已知PE或利润同比非正或无效，禁止新买入并退出；历史财报缺失允许价格信号回测，但不能视作通过基本面审查 |
| 超跌消退 | 不再加仓；若广泛深度超跌状态结束，每日减回45%以内，不等待下次调仓 |
| 反弹退出 | 前一交易日收盘价回到MA20，或RSI14≥50，清仓 |
| 止损与时间退出 | 前一收盘价相对含手续费平均成本下跌≥8%，或自首次建仓起满20个组合交易日，清仓；加仓不重置持有期限 |

只在超跌条件成立且评分器确认买入时建仓/加仓。低估值、主题强势、上涨突破单独出现都不构成买入理由。DeepSeek可以否决候选或建议退出，不能改写量化门槛或风险上限。规则版Dashboard使用相同的门槛，避免依赖模型输出。

实时信号中的“目标权重”是总资产目标占比，**不是追加买入比例**。实际新增额度必须扣除该股票已有仓位与整个组合已有仓位；回测执行器已经计算这些约束。系统没有接入券商账户或自动下单，信号页并不知道用户真实持仓。

## 执行与数据边界

- 历史信号只看成交日以前的收盘价；历史财报只使用前一市场交易日已经可用的条目。不把今日财报快照带入历史。
- 模拟在当日收盘价成交，按100股整数手买入，计入双边手续费（配置`feeBps`为每边费率），卖出向上取整以满足仓位约束。这沿用原回测的简化撮合；未完整模拟各板块最小申报量、滑点、税费、成交量约束、除权持股调整和真实盘中订单。
- 每日先处理退出，再处理单股和总仓位超限，最后考虑新增买入。风控减仓不受旧策略的最短持有期或调仓偏差阈值限制；不在同一日对同一股票先卖再买。
- 停牌和跌停时不伪造成交，退出请求会持续重试。价格变动和无法成交可能造成被动超限，此时停止新增买入，并在每日记录中标记`riskBreach`。80%是主动持仓硬上限，不能保证市场跳空或无法减仓时的瞬时市值比例。
- 缺失行情的股票保留在覆盖率分母中；未跟上最近市场交易日的旧日线不能触发买入或开启80%额度。
- 股票池为当前成员，存在幸存者偏差；缓存数据有缺口，财报日期采用估算披露延迟，并非完整的历史公告数据库。历史模拟不能当作实盘业绩。
- 通用引擎保留显式`legacy-ranking`模式用于旧撮合回归测试和历史比较。默认引擎、网页API、Dashboard和快照生成均使用`oversold-v1`，网页不开放旧策略切换。静态站旧信号失效，旧回测标注为历史策略，不能冒充新策略结果。

RSI使用Wilder的14期平滑算法；平盘时取50。RSI低于30是常用超跌参考，但可能持续很久：[Fidelity RSI说明](https://www.fidelity.com/learning-center/trading-investing/technical-analysis/technical-indicator-guide/rsi)。组合比例、均线偏离、回撤、退出阈值均为本项目自定规则。

## 复现

```bash
cd web
npm run lint
npm test
./node_modules/.bin/tsc --noEmit
npm run build
DASHBOARD_CACHE=/path/to/datasource npx tsx scripts/build-dashboard.ts
```

`web/data/dashboard-backtest.json`包含逐日现金、持仓、市值占比、当日仓位上限、市场状态、超限标记和成交记录。`signalsByDate`仅保留买入候选；退出成交附带原因。实际行情截止日期见`latestDate`，不能以构建日期或请求区间终点冒充最新行情。调整买入检查周期可用`DASHBOARD_REBALANCE`，每日风险检查持续生效。

## 本次验证（2026-09-06）

- 96只股票的本地历史缓存，实际区间2024-01-02至2026-08-05，共627个组合日期；数据并非最新行情。
- 默认每日评估、100万元初始资金、每边10bps：总收益11.89%，年化4.43%，最大回撤7.02%，共306笔成交。这是简化历史模拟，不是实盘验证，也没有据此优化阈值。
- 逐日重算现金和持仓市值，常态最大仓位44.9983%，无超限记录；187笔买入均用成交日前数据复核超跌条件。样本中没有触发广泛深度超跌，80%建仓与退潮减仓由专门的模拟场景测试验证。
- 54项测试、ESLint、TypeScript和生产构建通过；本地生产服务的Dashboard与回测页面HTTP输出通过检查。
